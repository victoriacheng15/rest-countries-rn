import { View, Text } from "react-native";

export default function About() {
	return (
		<View>
			<Text>This page is to test to understand how navigate work on mobile app with expo router</Text>
		</View>
	);
}
